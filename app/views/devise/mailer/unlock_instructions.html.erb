<p><%= "#{t('devise.salutation.hello')} #{@resource.email}" %>!</p>

<p><%= t('devise.locked.info') %></p>

<p><%= t('devise.unlock.intro') %></p>

<p><%= link_to "#{devise.unlock.account}", unlock_url(@resource, unlock_token: @token) %></p>
