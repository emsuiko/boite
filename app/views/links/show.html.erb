<p>
  <strong><%= t('models.link.name') %>:</strong>
  <%= @link.name %>
</p>

<p>
  <strong><%= t('models.link.url') %>:</strong>
  <%= link_to link.url, link.url %>
</p>

<%= link_to t('crud.edit'), edit_link_path(@link) %> |
<%= link_to t('nav.back'), links_path %>
